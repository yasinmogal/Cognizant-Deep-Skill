Basic Docker Commands

docker --version

docker images

docker ps

docker ps -a

docker pull nginx

docker run nginx

docker stop <container-id>

docker rm <container-id>

docker rmi <image-id>

docker exec -it <container-id> bash